  
<% if activity.trackable %>  
<% book =  Book.find(activity.trackable.book_id) %>
<% user = User.find(book.user_id) %>
  <%=link_to user.first_name, user %>
  updated their review for <%= link_to book.name, book_path(book.code)  %>. Read it now!
<% else %>
  <% user = User.find(activity.owner_id) %>
  <%= link_to user.first_name, user_path(user) %> updated a review for a book but has since deleted it.
<% end %>