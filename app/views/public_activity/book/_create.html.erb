<%# Need a catch statement if the user does the action but then removes the book/review as it's no longer in the database %>
<% if activity.trackable %>
  <%=link_to activity.trackable.user.first_name, activity.trackable.user %>
  added <%= link_to activity.trackable.name, book_path(activity.trackable.code)  %> 
  to their library. Check out their other books.
<% else %>
  <% user = User.find(activity.owner_id) %>
  <%= link_to user.first_name, user_path(user) %> added a book to their library but has since removed it.
<% end %>
