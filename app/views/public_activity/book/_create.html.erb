

<% if activity.trackable %>
  <%= activity.trackable.user.first_name %>
  added <%= link_to activity.trackable.name, activity.trackable  %> 
  to their library
<% else %>
  <%= activity.trackable.user.first_name %> added a book to their library
  which has since been removed
<% end %>
